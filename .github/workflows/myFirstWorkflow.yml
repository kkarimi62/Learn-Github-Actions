name: my-firstworkflow

on: 
 push:
 schedule:
   - cron: 5 * * * *
 workflow_dispatch: 
 

jobs:
  my-first-job:
   name: My first job
   runs-on: ubuntu-latest
   steps:
     - name: step-1
       run: |
        pwd
        ls -ltr
        hostname
     - name: step-2 checkout
       uses: actions/checkout@v6.1.0
     - name: step-3
       run: git status
   

    
        
        
   
   
